var typingEffect = new Typed(".typedText", {
    strings: ["Sandu", "Sandu", "Sandu"], // Corrected property name
    loop: true,
    typeSpeed: 100,
    backSpeed: 80,
    backDelay: 2000, // Use a comma instead of a semicolon
});
